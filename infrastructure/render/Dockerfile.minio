FROM minio/minio:latest

ENV MINIO_ROOT_USER=ptw_minio \
    MINIO_ROOT_PASSWORD=ptw_minio_password

# Render injects PORT. Persist data on attached disk at /data.
ENTRYPOINT ["/bin/sh", "-c", "exec minio server /data --address :${PORT:-9000} --console-address :9001"]
